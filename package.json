{
  "name": "memory-game-backend",
  "version": "1.0.0",
  "description": "Backend API for Memory Sequence Game with SQL database",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "init-db": "mysql -u root -p < database.sql"
  },
  "keywords": ["memory", "game", "sql", "api", "express"],
  "author": "Memory Game Developer",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "mysql2": "^3.6.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
